/**
 *  class Application
**/
var Application = Class.create({
  /**
   *  new Application();
  **/
  initialize: function() {
    // Finds all slides elements (uses Slide objects)
    // Handles keyboard events
    // Delegates slide handling to custom functions
  },
  toString: function() { return "[object Application]"; }
});